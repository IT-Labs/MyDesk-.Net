namespace inOfficeApplication.Data.Utils
{

    public enum RoleTypes
    {
        ADMIN,
        EMPLOYEE,
        ALL
    }

    public enum AuthTypes
    {
        Azure,
        Google,
        Custom
    }

    public enum DbType
    {
        SQL,
        InMemory
    }
}
